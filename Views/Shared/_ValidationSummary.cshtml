@model ValidationSummaryModel


<div id="validation-summary">
    <ul>
        @foreach (var error in Model.Fields)
        {
            if (!error.IsValid)
            {
                <li hx-on:load="console.log('hello world')">@error.ErrorMessage</li>
            }
        }
    </ul>
</div>